<UserControl x:Class="TeamsVoiceWizardV2.Views.PhoneManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
             xmlns:tw="using:TeamsVoiceWizardV2">

    <UserControl.Resources>
        <tw:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid x:Name="RootGrid"
          Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal"
                    Spacing="8" Margin="0,0,0,8">
            <Button Content="Load Numbers"
                    Command="{Binding LoadNumbersCommand}"
                    IsEnabled="{Binding IsLoadNumbersEnabled}" />
            <Button Content="Apply Changes"
                    Command="{Binding ApplyBatchChangesCommand}" />
            <Button Content="Bulk Import…"
                    Command="{Binding OpenBulkImportCommand}" />
            <ProgressRing IsActive="{Binding IsPhoneBusy}"
                          Width="18" Height="18"
                          VerticalAlignment="Center" />
            <TextBlock VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       Text="{Binding PhoneStatusMessage}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <controls:DataGrid x:Name="NumbersGrid"
                               Background="Transparent"
                               Grid.Column="0"
                               AutoGenerateColumns="False"
                               SelectionMode="Single"
                               CanUserSortColumns="True"
                               IsReadOnly="True"
                               GridLinesVisibility="Horizontal"
                               ItemsSource="{Binding PhoneRecords}"
                               SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                               Sorting="NumbersGrid_Sorting">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Phone Number"
                                                 Binding="{Binding TelephoneNumber}"
                                                 Tag="TelephoneNumber"
                                                 Width="160" />
                    <controls:DataGridTextColumn Header="Type"
                                                 Binding="{Binding NumberType}"
                                                 Tag="NumberType"
                                                 Width="140" />
                    <controls:DataGridTextColumn Header="Assigned To"
                                                 Binding="{Binding AssignedUserDisplayName}"
                                                 Tag="AssignedUserDisplayName"
                                                 Width="220" />
                    <controls:DataGridTextColumn Header="Status"
                                                 Binding="{Binding AssignmentStatus}"
                                                 Tag="AssignmentStatus"
                                                 Width="*"
                                                 MinWidth="200" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>

            <Border Grid.Column="1"
                    Width="320"
                    BorderThickness="1,0,0,0"
                    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    Padding="12"
                    Visibility="{Binding IsSidePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16">
                        <StackPanel Spacing="8">
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="Phone Number Details"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Style="{StaticResource BaseTextBlockStyle}" />
                                <Button Grid.Column="1"
                                        AutomationProperties.Name="Close details panel"
                                        ToolTipService.ToolTip="Close"
                                        Command="{Binding CloseSidePanelCommand}"
                                        Padding="8,4"
                                        MinWidth="32"
                                        Style="{StaticResource SubtleButtonStyle}">
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              Glyph="&#xE711;"
                                              FontSize="14" />
                                </Button>
                            </Grid>

                            <Grid RowSpacing="6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="Number:"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                <TextBlock Grid.Row="0" Grid.Column="1"
                                           Text="{Binding DetailPhoneNumber}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           FontFamily="Consolas" />

                                <TextBlock Grid.Row="1" Grid.Column="0"
                                           Text="Type:"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                <TextBlock Grid.Row="1" Grid.Column="1"
                                           Text="{Binding DetailNumberType}"
                                           Style="{StaticResource CaptionTextBlockStyle}" />

                                <TextBlock Grid.Row="2" Grid.Column="0"
                                           Text="Status:"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                <TextBlock Grid.Row="2" Grid.Column="1"
                                           Text="{Binding DetailStatus}"
                                           Style="{StaticResource CaptionTextBlockStyle}" />

                                <TextBlock Grid.Row="3" Grid.Column="0"
                                           Text="Current User:"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                <TextBlock Grid.Row="3" Grid.Column="1"
                                           Text="{Binding DetailCurrentUser}"
                                           Style="{StaticResource CaptionTextBlockStyle}" />
                            </Grid>
                        </StackPanel>

                        <Border Height="1"
                                Background="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Opacity="0.3" />

                        <StackPanel Spacing="8" Visibility="Visible">
                            <TextBlock Text="Assign User"
                                       FontWeight="SemiBold"
                                       Style="{StaticResource BaseTextBlockStyle}" />

                            <StackPanel Spacing="4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Teams Phone User:"
                                               VerticalAlignment="Center"
                                               Style="{StaticResource CaptionTextBlockStyle}" />
                                    <ProgressRing IsActive="{Binding UserLoadingRingActive}"
                                                  Width="14"
                                                  Height="14"
                                                  VerticalAlignment="Center" />
                                </StackPanel>

                                <ComboBox HorizontalAlignment="Stretch"
                                          PlaceholderText="Select a user"
                                          ItemsSource="{Binding UserOptions}"
                                          DisplayMemberPath="Label"
                                          SelectedItem="{Binding SelectedUserItem, Mode=TwoWay}"
                                          DropDownOpened="UserComboBox_DropDownOpened" />
                            </StackPanel>
                        </StackPanel>

                        <Border Height="1"
                                Background="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Opacity="0.3" />

                        <StackPanel Spacing="8"
                                    Visibility="{Binding IsPolicySectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Direct Routing Policies"
                                       FontWeight="SemiBold"
                                       Style="{StaticResource BaseTextBlockStyle}" />

                            <StackPanel Spacing="4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Dial Plan:"
                                               VerticalAlignment="Center"
                                               Style="{StaticResource CaptionTextBlockStyle}" />
                                    <ProgressRing IsActive="{Binding DialPlanLoadingRingActive}"
                                                  Width="14" Height="14"
                                                  VerticalAlignment="Center" />
                                </StackPanel>

                                <ComboBox HorizontalAlignment="Stretch"
                                          PlaceholderText="None"
                                          ItemsSource="{Binding DialPlanOptions}"
                                          DisplayMemberPath="Label"
                                          SelectedItem="{Binding SelectedDialPlanItem, Mode=TwoWay}"
                                          DropDownOpened="DialPlanComboBox_DropDownOpened" />
                            </StackPanel>

                            <StackPanel Spacing="4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Voice Routing Policy:"
                                               VerticalAlignment="Center"
                                               Style="{StaticResource CaptionTextBlockStyle}" />
                                    <ProgressRing IsActive="{Binding VRPolicyLoadingRingActive}"
                                                  Width="14" Height="14"
                                                  VerticalAlignment="Center" />
                                </StackPanel>

                                <ComboBox HorizontalAlignment="Stretch"
                                          PlaceholderText="None"
                                          ItemsSource="{Binding VoiceRoutingOptions}"
                                          DisplayMemberPath="Label"
                                          SelectedItem="{Binding SelectedVoiceRoutingItem, Mode=TwoWay}"
                                          DropDownOpened="VoiceRoutingPolicyComboBox_DropDownOpened" />
                            </StackPanel>
                        </StackPanel>

                        <Border Height="1"
                                Background="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                Opacity="0.3" />

                        <Button Content="Apply Changes"
                                HorizontalAlignment="Stretch"
                                Command="{Binding ApplySingleChangeCommand}"
                                Style="{StaticResource AccentButtonStyle}" />

                        <TextBlock Text="{Binding SidePanelStatusText}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{Binding SidePanelStatusBrush}"
                                   TextWrapping="Wrap"
                                   Visibility="{Binding SidePanelStatusVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
